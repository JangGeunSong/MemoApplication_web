export type MemoProps = {
    idx: number;
    title: string;
    description: string;
    background: string;
    onClick: Function;
}

export type ModalProps = {
    title: string;
    description: string;
    background: string;
    onClick: Function;
}